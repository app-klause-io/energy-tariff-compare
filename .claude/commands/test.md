Run the full test suite and report results.

1. Run `pnpm lint` — Prettier + ESLint check
2. Run `pnpm check` — TypeScript + svelte-check
3. Run `pnpm test:unit` — Vitest unit tests
4. Run `pnpm build` — production build

Report any failures with file paths and error messages. Fix lint/type errors before moving on.
